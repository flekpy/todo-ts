export function checkColorTag(color: string): string {
  if (color === '#D2CEFF') {
    return color;
  } if (color === '#D1E5F7') {
    return color;
  } if (color === '#FFCECE') {
    return color;
  }
  return color;
}
